# `get_time_samples`

This node returns the time codes at which the attribute has authored time samples.

## Inputs

### `attribute`
The attribute to query.

## Outputs

### `success`
Boolean indicating whether the attribute has time samples.

### `time_samples`
The times (in ascending order) for which the attribute has samples.
